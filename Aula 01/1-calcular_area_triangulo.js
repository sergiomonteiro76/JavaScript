//Calcular a área de um triângulo:
// Variáveis
const base = 10;
const altura = 5;

// Cálculo da área
const area = (base * altura) / 2;

// Exibir resultado
console.log("A área do triângulo é:", area);